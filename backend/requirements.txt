# Core
langgraph>=0.2.0
langchain-groq>=0.2.0
langchain-core>=0.3.0

# Web framework
fastapi>=0.109.0
uvicorn>=0.27.0
jinja2>=3.1.0  # HTML templates for poll pages
python-multipart>=0.0.6  # Form data parsing for poll votes

# Observability
weave>=0.51.0

# Hosting
modal>=0.66.0

# API clients
httpx>=0.27.0

# Web automation (TinyFish)
# Uses httpx (already listed above) to call TinyFish API

# Data validation
pydantic>=2.0.0

# Code execution (for polls)
restrictedpython>=7.0

# Utilities
python-dotenv>=1.0.0
pytz>=2024.1  # Timezone handling

# Firebase Admin SDK
firebase-admin>=6.0.0

# Redis checkpointer for LangGraph persistence
langgraph-checkpoint-redis>=0.0.1

# Redis client for direct operations (user preferences)
redis>=5.0.0

# Stripe payments
stripe>=8.0.0

# Slack integration
slack-bolt>=1.18.0
slack-sdk>=3.27.0

# Google Calendar integration
google-auth>=2.28.0
google-api-python-client>=2.120.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# PDF generation (expense receipts)
reportlab>=4.1.0
